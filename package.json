{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "web": "lerna --scope=react-design-system run dev --stream --",
    "expo": "lerna --scope=react-native-design-system run start --stream --"
  },
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
